/**
 * 全局模板库 - TypeScript 类型定义
 * 
 * 定义了模板数据结构、分类和国家选项的类型
 * 包含关键修正：
 * 1. countryFlag 字段 - 存储国旗emoji
 * 2. completeness 字段 - 存储完成度百分比（替代原size字段）
 */

/**
 * 模板接口
 * 描述单个监管模板的完整数据结构
 */
export interface Template {
  /** 唯一标识符 */
  id: string;
  
  /** 模板名称（如："510(k) Premarket Notification"） */
  name: string;
  
  /** 国家/地区名称（如："United States"） */
  country: string;
  
  /** 国家/地区代码（如："US", "EU"） */
  countryCode: string;
  
  /** 国旗emoji（如："🇺🇸"） - 关键修正1 */
  countryFlag: string;
  
  /** 监管机构（如："FDA", "EMA"） */
  authority: string;
  
  /** 模板分类 */
  category: Category;
  
  /** 模板描述 */
  description: string;
  
  /** 发布/更新日期（格式：YYYY-MM-DD） */
  date: string;
  
  /** 完成度百分比（0-100的整数） - 关键修正2 */
  completeness: number;
  
  /** 文件格式（如："PDF", "DOCX"） */
  format: string;
  
  /** 是否为官方模板 */
  isOfficial: boolean;
  
  /** 是否为精选模板 */
  isFeatured: boolean;
  
  /** 下载链接 */
  downloadUrl: string;
}

/**
 * 模板分类类型
 * 用于筛选不同类型的监管模板
 */
export type Category = 
  | "All Categories"          // 所有分类
  | "Medical Device"           // 医疗器械
  | "Drug/Pharmaceutical"      // 药品/制药
  | "Biologic";                // 生物制品

/**
 * 国家/地区类型
 * 包含国旗emoji前缀 - 关键修正1
 * 用于筛选不同国家/地区的监管模板
 */
export type Country = 
  | "All Countries"                    // 所有国家
  | "🇺🇸 United States (FDA)"          // 美国 - 食品药品监督管理局
  | "🇪🇺 European Union (EMA)"         // 欧盟 - 欧洲药品管理局
  | "🇯🇵 Japan (PMDA)"                 // 日本 - 医药品医疗器械综合机构
  | "🇨🇦 Canada (Health Canada)"       // 加拿大 - 加拿大卫生部
  | "🇦🇺 Australia (TGA)"              // 澳大利亚 - 治疗用品管理局
  | "🇨🇳 China (NMPA)"                 // 中国 - 国家药品监督管理局
  | "🇧🇷 Brazil (ANVISA)"              // 巴西 - 国家卫生监督局
  | "🇮🇳 India (CDSCO)"                // 印度 - 中央药品标准控制组织
  | "🇰🇷 South Korea (MFDS)"           // 韩国 - 食品药品安全处
  | "🇷🇺 Russia (Roszdravnadzor)";     // 俄罗斯 - 联邦卫生监督局

